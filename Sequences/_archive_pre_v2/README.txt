PRE-V2 PROMPT ARCHIVE
=====================
Snapshot of the *.txt prompt files in each Sequences/Sequence_NN/ folder as of
2026-06-10, immediately before the v2 reset.

These prompts were written against the OLD spec:
  - Asuras as ONE generic group (or split into 3 variants — Foot-Soldier,
    Brute, Commander). The current spec uses 5 archetypes.
  - Ganas as ONE generic group. The current spec uses 4 Dharma Army archetypes.
  - NO cultural anchor (the GLOBAL_CULTURAL_TOKEN didn't exist yet).
  - NO anti-cloning token.
  - NO environment locked blocks.
  - NO Bengal Tiger / Sequence_20.5 / weapons-catalog references.
  - Karuppar described with turban + moustache (now: loose hair + beard).
  - Shiva described with the "tiny furl + bead of fire" model (now:
    thigh-strike).

Kept here purely for reference / rollback. Do NOT use as the source of truth.

The live spec is: Script.txt + CHARACTER_BIBLE.txt + ENVIRONMENT_BIBLE.txt +
CONFIG.txt + SEQUENCE_BACKPATCH_SPEC.txt. The live engine is
Sequences/Generate-sequences.txt.
