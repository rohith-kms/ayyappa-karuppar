Bengal Tiger — Ayyappa's Divine Mount  (NEW)
============================================
The royal Bengal tigress that serves as Lord Ayyappa's sacred mount in the
Sabarimala tradition. Replaces no previous subject — this is a new addition
that closes a gap in the original spec.

Master plate: Bengal-Tiger_master_sheet.png (canonical, sourced from the
team's Final-images CHAR_BENGAL_TIGER_MASTER_BIBLE_v02_LOCKED.png).

LOCKED BLOCK in /CHARACTER_BIBLE.txt §2 — copy verbatim into every prompt
that features the tiger.

The plate carries an "ABSOLUTE IDENTITY LOCK" — face pattern, stripe layout,
forehead pattern, muzzle structure, fur coloration, eye shape & color. Do
NOT mutate any of these between shots.

USE: Ayyappa's mount in any beat where he appears mounted. The current
22-beat script does not yet feature a mounted Ayyappa — the producer may add
a beat (see Script.txt) before sequence generation if desired.
